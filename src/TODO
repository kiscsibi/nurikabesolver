* check the whole loadlevel thing if everything works correctly
  and if all exceptions are caught by the gui
  
* program generator
* program solver



Method details:

* TCell; up down left right, 
* getpossibilities()?

* TBoard; CheckReachable() use A*?


